import React from 'react';
import ReactDOM from 'react-dom/client';
import VirtualContrastCT from './virtual-contrast-ct';
import './index.css';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <VirtualContrastCT />
  </React.StrictMode>
);
